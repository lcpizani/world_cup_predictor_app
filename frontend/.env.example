# URL of the FastAPI backend — used by client-side fetch (must be NEXT_PUBLIC_)
NEXT_PUBLIC_API_URL=
# Same URL used by Next.js Route Handlers on the server (no NEXT_PUBLIC_ prefix needed)
BACKEND_URL=